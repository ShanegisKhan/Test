#include <stdio.h>
#include <stdlib.h>

void usage(char *);

int main(int argc, char *argv[])
{
    int i, count;
    
    if(argc < 3) {
        usage
    } else {
        count = atoi(argv[2]
    }
}
